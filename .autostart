# cursor bar or bold pipe
# echo -e "\e[2 q"

set -gx COLUMNS LINES

# cursor underline
echo -e "\e[4 q"

# cursor pipe
# echo -e "\e[6 q"

# c

# if test "$TMUX" = ""
#   tmux
# end

# if test ! -f ~/.scripts/pid/login
#   bash ~/.scripts/login.sh
# end

# neo
# clear
rxfetch

# SSH for Git & GitHub!
# set SSH_ENV "${HOME}/.ssh/agent-environment"

# function start_agent
#   termux-toast -b "#FFFFFF" -c "#000000" -g top "Initialising new SSH Agent" &> /dev/null
#   ssh-agent | sed 's/^echo/#echo/' > "${SSH_ENV}"
#   termux-toast -b "#FFFFFF" -c "#000000" -g top "Succeeded" &> /dev/null
#   chmod 600 "${SSH_ENV}"
#   . "${SSH_ENV}" > /dev/null
#   ssh-add
# end

# if test -f "${SSH_ENV}"
#   . "${SSH_ENV}" > /dev/null
#   ps -ef | grep ${SSH_AGENT_PID} | grep ssh-agent$ > /dev/null
#   or start_agent
# else
#   start_agent
# end
